apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azerothauctionassassin.fullname" . }}-test-connection"
  labels:
    {{- include "azerothauctionassassin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azerothauctionassassin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
